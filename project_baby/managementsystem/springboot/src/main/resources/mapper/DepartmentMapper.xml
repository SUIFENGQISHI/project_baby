<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.DepartmentMapper">
    <insert id="insert" parameterType="com.example.entity.Department">
        insert into department(name)
        values( #{name})
    </insert>
    <delete id="deleteById" parameterType="com.example.entity.Department">
        delete from department
        where id = #{id}
    </delete>
    <update id="updateById" parameterType="com.example.entity.Department">
        update department
        set name = #{name}
        where id = #{id}
    </update>
    <select id="selectAll" resultType="com.example.entity.Department">
        select *
        from department
        <where>
            <if test="name != null and name != ''">
                name like concat('%', #{name}, '%')
            </if>
        </where>
        order by id desc
    </select>
    <select id="selectById" resultType="com.example.entity.Department">
        select *
        from department
        where id = #{id}
    </select>
</mapper>